[package]
name = "geo-polygonizer"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"

[dependencies]
geo = "^0.33.0"
rstar = "0.12"

[dev-dependencies]
geojson = { version = "^1.0.0", features = [ "geo-types" ] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

